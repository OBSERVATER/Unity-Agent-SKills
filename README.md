# AI Skills (com.observater.aiskills)

**AI Skills** 是一个基于 Python.NET 的 Unity Editor 自动化扩展包。它允许用户通过自然语言指令，驱动 Unity 内部 API 执行复杂的编辑器任务，如场景构建、资源管理、UI 生成等。

该项目利用 LLM (大语言模型) 将自然语言转换为 Python 脚本，并直接在 Unity 编辑器进程中运行。

## ✨ 主要特性 (Features)

根据当前集成的技能模块，本插件支持以下核心能力：

- **物体管理 (GameObject)**
  - 创建基本几何体 (Cube, Sphere 等) 或空物体。
  - 批量设置位置、旋转和缩放。
  - 查找与删除层级对象。
- **组件系统 (Component)**
  - 智能添加或获取组件 (Rigidbody, BoxCollider, AudioSource 等)。
  - 自动配置组件属性（如刚体质量、重力、运动学状态）。
- **资源管线 (Asset Pipeline)**
  - 基于过滤器 (Filter) 查找项目资源 (Material, Prefab, Texture)。
  - 移动、重命名或管理资源路径。
  - **注意**: 所有路径均基于 `Assets/` 根目录。
- **预制体工作流 (Prefab)**
  - 将场景物体保存为预制体 (SaveAsPrefabAsset)。
  - 实例化预制体并指定坐标。
- **UI 自动生成 (User Interface)**
  - 一键生成标准 UI 结构 (Canvas -> Text/Image/Button)。
  - 自动处理 RectTransform 布局与层级关系。
  - 支持 Text, Image, Button, Slider 等常用控件。
- **历史记录管理 (History Management)**
  - 支持保存、导入与清除历史记录。
- **提示管理 (Prompt Management)**
  - 增强提示管理功能，支持更高效的处理。

## 📦 安装 (Installation)

### 通过 Package Manager 安装

1. 打开 Unity，点击 `Window > Package Manager`。
2. 点击左上角 `+` 号，选择 `Add package from git URL...`。
3. 输入本仓库的 Git 地址。

## 🛠️ 使用指南 (Usage)

本插件通过加载 `.md` 格式的 "Skills" 文件来定义 AI 的能力边界。

1. **输入指令**: 在 AI Skills 窗口输入自然语言，例如：
   > "在 (0,0,0) 创建一个名为 Player 的胶囊体，添加刚体组件，并保存为 Prefab。"
2. **生成与执行**: 系统会根据 [unity.md] 定义的核心规则生成 Python 代码：
   ```python
   import UnityEngine
   import UnityEditor
   # 自动生成的逻辑...
   结果反馈: 操作完成后，Console 会输出执行日志。
   ```

⚙️ 技术约束 (Technical Constraints)
如果您计划扩展本包的技能，请遵循 unity.md 定义的核心规则与Claude Code Markdown编写规范：

执行环境: 代码运行在 Python.NET 环境中，可以直接调用 C# API。

命名空间: 必须显式 import 并使用完整命名空间（如 UnityEngine.GameObject），防止类型冲突。

代码结构: 只能输出一个代码块，且脚本末尾必须直接调用入口函数。

禁止项: 禁止使用 if **name** == "**main**": 守护代码。

📂 目录结构 (Package Structure)
```Plaintext
com.observater.aiskills
├── package.json # 包清单
├── README.md # 文档
├── Documentation # 技能定义文档
│ ├── unity.md # 核心规则
│ ├── unity-gameobject.md
│ ├── unity-component.md
│ ├── unity-asset.md
│ ├── unity-prefab.md
│ └── unity-ui.md
├── Editor # 编辑器逻辑 (C#)
│ └── ...
└── Runtime # 运行时逻辑 (C# / Python Bridge)
└── ...
```